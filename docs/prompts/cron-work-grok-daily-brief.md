# Grok — daily system brief (work only)

The orchestrator has already created this run’s Paperclip issue and recorded `started` in `data/cron-runs/*.jsonl`. **Do not** call `cron-paperclip-lifecycle.sh`, `cron-run-record.sh`, or create another Paperclip run issue; the wrapper will record the terminal outcome and close Paperclip.

You are Grok. Read `memory/MEMORY.md` in full. Produce the Grok Daily System Brief for the last 24 hours of GrokClaw. Review Paperclip issues from the last 24 hours, read `data/cron-runs/*.jsonl` for execution history, inspect audit logs in `data/audit-log/*.jsonl` if present, inspect `data/linear-creations/*.jsonl` and flag any flow outside `suggestion` or `user_request`, run read-only health checks for gateway, Paperclip, Telegram, and Ollama, and read today's agent reports. Summarize what succeeded, what failed, what needs fixing today, and one concrete way to improve the system. If you have a concrete improvement worth approval, post it with `./tools/telegram-suggestion.sh <next-N> "<title>" "<reasoning>" "<impact>" "<description>"`. Otherwise post the brief to suggestions: `printf '%s\n' 'Daily system brief: <summary>' | ./tools/telegram-post.sh suggestions` (avoids shell expanding `$` in metrics).
